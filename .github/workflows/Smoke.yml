name: Playwright Smoke Tests

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:
  schedule:
    - cron: '30 1 * * *'   # Runs at 7:00 AM IST daily (01:30 UTC)
    - cron: '40 15 * * *'  # Runs at 21:10 IST daily (15:40 UTC)

jobs:
  smoke-tests:
    runs-on: ubuntu-latest
    timeout-minutes: 60

    steps:
      # Checkout code
      - name: Checkout Repository
        uses: actions/checkout@v4

      # Setup Node
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'

      # Install dependencies
      - name: Install dependencies
        run: npm ci

      # Install Playwright browsers
      - name: Install Playwright browsers
        run: npx playwright install --with-deps

      # Run Smoke tests
      - name: Run Smoke tests
        run: |
          npx playwright test --grep "@Smoke" --reporter=list,html
        continue-on-error: false

      # Upload Playwright HTML report
      - name: Upload Test Report
        uses: actions/upload-artifact@v4
        if: ${{ always() }}
        with:
          name: smoke-report
          path: playwright-report/
          retention-days: 30

      # Optional: Slack notification (uncomment if needed)
      #- name: Notify Slack
      #  uses: slackapi/slack-github-action@v1
      #  with:
      #    channel-id: 'YOUR_CHANNEL_ID'
      #    slack-token: ${{ secrets.SLACK_TOKEN }}
      #    text: "Playwright smoke tests completed. Check artifacts for details."

